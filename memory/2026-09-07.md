# 2026-09-07 — MVP realignment (§16): sesión de cierre (steps 17-19 + migración)

Completada la checklist §16 del MVP. `git status`: 127 cambios (116 files, +2988/-10432). No commits hechos.

## Lo hecho hoy (cierre)
- **Paso 17 (tests §15)**: borrados `test/features/data_room/**` (8 archivos), `test/services/conversion_service_test.dart`, `test/utils/office_formats_test.dart` (trash en `~/AppData/Local/Temp/opencode/trash_tests/`). Borrado `lib/app/constants/storage_constants.dart` + su test (dead, valores legacy 10MB/48h que contradecían el contrato 20MB/168h; trash en `trash_lib_app_constants/`).
- `test/models/kripton_file_test.dart` reescrito: round-trip, bytea (array/hex/base64/hex impar→FormatException), contexto recipient, ShareLink, EncryptedPayload. Sin campos de preview.
- Creados: `test/utils/supported_formats_test.dart`, `test/models/user_model_test.dart` (5 casos effectiveTier + límites), `test/core/localization/paywall_trigger_keys_test.dart` (mapeo 6 triggers→claves ARB en los 5 locales, lee JSON desde disco).
- `flutter analyze`: **No issues found**. `flutter test`: **54/54 passed**.
- **E2E_TEST_GUIDE.md** reescrito completo (sin Fase 1 ni VDR; §15.2: docx bloqueado, límites free/premium/business, trial expirado, purchase mock, first_recipient_view, regresión Office).
- **README.md** reescrito: zero-knowledge, formatos soportados, tabla de planes/precios, trial, sin Gotenberg/Docker/anuncios/VDR.
- **Paso 19 (logo)**: `splash_screen.dart` y `auth_screen.dart`: placeholder verde "K" sustituido por `assets/branding/kriptonshare_logo_primary.png` (height 160 splash / 100 auth). `pubspec.yaml` ya declaraba `assets/branding/`. Sin 'K' restante en lib.

## Migración SQL aplicada en REMOTE (importante)
- Proyecto: `olskjkbyzpowxlhjhovu` (kriptonshare). Conecta con `supabase db push`/`migration` (CLI 2.111). Docker NO corre en esta máquina (warnings de catálogo inofensivos al final del push).
- Migración `20260907000000_mvp_realignment.sql` aplicada tras reparar BUGS del archivo original (primera vez que corría contra la DB real; antes solo se había diseñado):
  1. Drop-order: `files.folder_id` se dropeaba antes que la policy `files_public_read_active_folder_link` → movida a sección 3.
  2. `DROP POLICY ... folders` tras `DROP TABLE folders CASCADE` → falla si folders ya no existe; eliminado (CASCADE cubre).
  3. Funciones con RETURNS cambiado no aceptan `CREATE OR REPLACE` → añadido `DROP FUNCTION IF EXISTS` previo a `get_shared_file_metadata(UUID)`, `get_received_files()`, `check_upload_limits(UUID, INTEGER)` (firma legacy!) + `(UUID,BIGINT)`, `validate_share_link_expiration(UUID,TIMESTAMPTZ)`, `log_first_recipient_view(UUID)`.
  4. `INSERT ... VALUES ... WHERE NOT EXISTS` (SQL inválido) → `INSERT ... SELECT ... WHERE NOT EXISTS`.
  5. `RETURNS TABLE file_size_bytes BIGINT` NO coincide con la tabla `files.file_size_bytes INTEGER` (runtime 42804) → cambiado a INTEGER en ambos metadata RPCs.
- Para re-aplicar tras editar el archivo: `supabase migration repair --status reverted 20260907000000` + `supabase db push`. Remote history quedó reconciliada: 8 migraciones previas marcadas applied en `supabase_migrations` (tabla nueva del CLI; estaban solo en `_supabase_migrations` legacy), vía `migration repair --status applied`.
- Verificado: `get_received_files` → HTTP 200 (antes 400/42804). `migration list` muestra las 9 = Local y Remote.

## Decisiones / notas
- Los archivos viejos borrados se guardan en trash bajo `C:/Users/horac/AppData/Local/Temp/opencode/` (NO `rm` definitivo).
- CLAVE: en esta DB los límites de columna: `files.file_size_bytes` = INTEGER; `users.max_file_size_bytes` = BIGINT. Al crear RPCs con `RETURNS TABLE` hay que respetar los tipos reales de las columnas o PostgREST devuelve 42804 en runtime.
- Ruta de RPC legacy `check_upload_limits(p_file_size INTEGER)` — cualquier update futuro debe DROP del overload INTEGER o Postgres crea dos y PostgREST falla por ambigüedad.
- No hay docker daemon local: `supabase db push` funciona igual para remote; solo omite el caché del catálogo de migraciones (warning).

## Pendiente (fuera del MVP-§16)
- Nada bloqueante. Resta solo: probar E2E real en dispositivos (§15.2), regenerar iconos/splash nativos si se desea, y revisar que el visor recibe link de datos reales.
