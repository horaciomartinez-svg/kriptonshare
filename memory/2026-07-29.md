# 2026-07-29

## Tarea
Aplicar la actualización de arquitectura del Virtual Data Room (VDR) para usuarios Premium de KRIPTONSHARE según `KRIPTONSHARE Actualización Arquitectura Virtual Data Room 29Jul26.md`, e integrar tests + guía E2E.

## Fases implementadas
1. **Migración SQL y Edge Function**
   - `supabase/migrations/20260729000000_vdr_architecture_update.sql`: tablas `data_rooms`, `folders`, `files`, `share_links`, `journey_telemetry`, columnas Premium, políticas RLS, triggers y funciones RPC (`create_data_room`, `get_data_room_by_link`, etc.).
   - `supabase/functions/billing-sync/index.ts`: Edge Function para sincronizar eventos de RevenueCat y actualizar `max_storage_bytes`/`is_premium`.
2. **Core cliente**
   - Constantes VDR, tema corporativo Charcoal Black + Neon Green, utilidades crypto/network, datasource R2 (`r2_storage_datasource.dart`).
3. **Dominio y datos**
   - Entidades: `DataRoomEntity`, `FolderEntity`, `FileEntity`, `ShareLinkEntity`, `JourneyTelemetryEntity`.
   - Legacy movido a `legacy_file_entity.dart` para no romper el repositorio histórico.
   - Modelos, datasources (`supabase_data_source.dart`, `local_database_datasource.dart`), repositorios (`data_room_repository_impl.dart`, `folder_repository_impl.dart`, `crypto_repository_impl.dart`) y use cases.
4. **Notifiers**
   - `lazy_decryption_notifier.dart`, `upload_batch_notifier.dart`, `data_room_explorer_notifier.dart`, `folder_notifier.dart`.
5. **UI/UX**
   - Pantallas: `data_room_explorer_screen.dart`, `data_room_lobby_screen.dart`, `storage_management_screen.dart`.
   - Widgets Atomic Design: `atoms/`, `molecules/`, `organisms/`, `templates/`.
   - Watermark dinámico, layout seguro, indicadores de cifrado, gauge de storage.
6. **Enrutamiento e integración**
   - Rutas `/data-room`, `/f/:id` (folder share link) y `/d/:id` (data room share link) en `router_provider.dart`.
   - Integración con providers existentes y `data_room_providers.dart`/`folder_providers.dart`.
7. **Tests y guía E2E**
   - Tests unitarios para entidades, use cases, notifiers y repositorios VDR.
   - Actualización de `E2E_TEST_GUIDE.md` con sección VDR.

## Decisiones clave
- Las entidades legacy se aislaron en `legacy_file_entity.dart`; las nuevas entidades VDR usan los nombres exactos del documento.
- Se añadieron políticas RLS públicas para carpetas/archivos a través de enlaces activos, evitando funciones RPC adicionales para lectura pública.
- La contraseña de compartir en `DataRoomExplorerScreen` es un placeholder constante; la IP del receptor es `'0.0.0.0'` porque el proyecto no depende de `network_info_plus`.

## Verificación pendiente
- `flutter analyze` y `flutter test` no se ejecutaron desde este entorno Windows/PowerShell 5.1 (la herramienta Bash inserta `&&` incompatible). El usuario debe correrlos localmente.

## Acciones pendientes del usuario
1. Aplicar la migración SQL en Supabase.
2. Desplegar `supabase/functions/billing-sync`.
3. Configurar webhook de RevenueCat apuntando al Edge Function.
4. Reemplazar placeholders de contraseña de compartir e IP del receptor si se requiere comportamiento real.
5. Ejecutar `flutter pub get`, `flutter analyze`, `flutter test` y `flutter build apk --debug`.
6. Seguir la sección VDR de `E2E_TEST_GUIDE.md` para prueba end-to-end.
