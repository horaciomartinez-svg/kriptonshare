# KRIPTONSHARE — 2026-07-23

## Problema reportado
El receptor no ve archivos recibidos en el Dashboard, aunque el link aún no haya caducado. La sección "Archivos recibidos" aparece vacía.

## Causas identificadas
1. La función RPC `get_received_files()` solo existía en `supabase/test_e2e_setup.sql`, no en `supabase/schema.sql` ni en `supabase/schema_migration_fix.sql`. Si la base de datos de producción no la tenía, la RPC fallaba o devolvía vacío.
2. La función comparaba `sl.recipient_email = auth.email()` de forma case-sensitive. Si el email se guardó con mayúsculas/minúsculas diferentes, no coincidía.
3. Las políticas RLS de `share_links` y `files` solo permitían al creador (`owner_id = auth.uid()` / `created_by = auth.uid()`), impidiendo una consulta directa de fallback.

## Cambios realizados
- `supabase/test_e2e_setup.sql`:
  - Comparación de emails case-insensitive en `get_received_files()`: `LOWER(sl.recipient_email) = LOWER(auth.email())`.
  - Agregadas políticas `link_recipient_access` y `file_recipient_access` para receptores.
- `supabase/schema.sql`:
  - Agregada función `get_received_files()` con comparación case-insensitive.
  - Agregadas políticas `link_recipient_access` y `file_recipient_access`.
- `supabase/schema_migration_fix.sql`:
  - Agregadas las mismas políticas y función (numeración 6 y 7), para aplicar en bases de datos existentes.
- `lib/providers/file_provider.dart`:
  - `getReceivedFiles()` ahora loguea el email del usuario y la respuesta de la RPC.
  - Si la RPC falla o devuelve vacío, intenta un fallback directo a `share_links` con join a `files`.
  - El fallback aplana la respuesta anidada para construir `KriptonFile` correctamente.

## Pendiente / Acción requerida del usuario
1. Ejecutar `supabase/schema_migration_fix.sql` en el SQL Editor de Supabase (o aplicar la migración correspondiente) para crear `get_received_files()` y las políticas de receptor en la base de datos de producción.
2. Volver a compilar y probar en el dispositivo Android.
3. Verificar en los logs de VS Code que aparezca `[getReceivedFiles] RPC returned N files` o `[getReceivedFiles] Fallback response: N rows`.

## Soporte de imágenes y documentos Office

### Pregunta del usuario
¿La app ya está programada para visualizar imágenes y documentos de Microsoft Office de forma segura (sin descargar ni capturas)?

### Respuesta técnica
- **Imágenes**: sí, se visualizan con `Image.memory` + `InteractiveViewer`.
- **Microsoft Office (Word, Excel, PowerPoint)**: no. El visor solo muestra una pantalla de "Formato protegido"; no renderiza el contenido.
- **PDF**: sí, con `pdfrx` nativo dentro de la app.

### Problemas de seguridad encontrados y corregidos
1. El visor de imágenes podía mostrar el menú contextual nativo de guardar/compartir al mantener presionada la imagen.
   - Solución: envolver `Image.memory` en un `GestureDetector` con `onLongPressStart` para interceptar el menú.
2. El `AppBar` del visor PDF tenía un botón "Abrir con otra aplicación".
3. El fallback de PDF mostraba un botón "Abrir con otra aplicación".
   - Solución: eliminar ambos botones; el fallback ahora solo muestra un mensaje y un botón "Volver al inicio".
4. Se eliminó código muerto asociado: `_openWithExternalApp`, `_prepareTempPdfFile`, `_deleteTempPdfFile`, `_tempPdfFile`, e imports de `dart:io`, `open_filex`, `path` y `path_provider` desde `viewer_screen.dart`.
5. Se actualizó el mensaje para formatos no soportados (Office) explicando que deben convertirse a PDF antes de subirse.

### Archivo modificado
- `lib/screens/viewer/viewer_screen.dart`

## Módulo Premium y Data Room

### Implementación realizada
Se implementó la base técnica del módulo Premium/Data Room según `KRIPTONSHARE_Premium_DataRoom_Especificacion_Tecnica.md`.

#### Base de datos y backend
- `supabase/migrations/20260725000000_premium_dataroom.sql`:
  - Agrega `total_storage_used_bytes` y `max_storage_bytes` a `public.users`.
  - Crea tabla `public.folders` (Data Rooms virtuales).
  - Agrega `folder_id` e `is_deleted` a `public.files`.
  - Agrega `folder_id` a `public.share_links`, hace `file_id` nullable y agrega restricción `chk_share_link_target`.
  - Crea tabla `public.journey_telemetry` con índice BRIN.
  - Agrega políticas RLS para `folders` y `journey_telemetry`.
  - Refactoriza `check_upload_limits()` con validación Premium (100 MB / archivo, capacidad acumulada) y Freemium.
- `supabase/functions/revenuecat-webhook/index.ts`:
  - Webhook server-to-server que actualiza `subscription_tier` y `max_storage_bytes` desde eventos de RevenueCat.
  - Valida header `Authorization: Bearer <REVENUECAT_WEBHOOK_AUTH>`.

#### Capa de dominio y datos
- `lib/models/user_model.dart`: campos `maxStorageBytes`, getter `storageUsageRatio`, `remainingPremiumStorageBytes` usa `maxStorageBytes`.
- `lib/providers/auth_provider.dart`: insert inicial incluye `max_storage_bytes`.
- `lib/core/utils/constants.dart` y `lib/utils/constants.dart`: nuevas clases `PremiumLimits` y `Pricing`.
- `lib/core/services/revenue_cat_service.dart`: servicio `RevenueCatServiceImpl` con initialize, offerings, compras, restore.
- Entidades y repositorio de carpetas:
  - `lib/features/data_room/domain/entities/folder_entity.dart`
  - `lib/features/data_room/domain/entities/journey_telemetry_entity.dart`
  - `lib/features/data_room/domain/repositories/i_folder_repository.dart`
  - `lib/features/data_room/data/datasources/folder_remote_datasource.dart`
  - `lib/features/data_room/data/repositories/folder_repository_impl.dart`
  - `lib/features/data_room/folder_providers.dart`
- `lib/services/crypto_service.dart`: nuevo método `decryptFileBytes` para Lazy Decryption.

#### Presentación
- `lib/features/data_room/presentation/notifiers/storage_upsell_notifier.dart`: estado de compras RevenueCat.
- `lib/features/data_room/presentation/screens/storage_management_screen.dart`: gauge de consumo, CTA de add-on, precios, botón Restaurar Compras.
- `lib/features/data_room/presentation/notifiers/folder_notifier.dart`: Lazy Decryption (descarga/descifra un solo archivo, evapora RAM).
- `lib/features/data_room/presentation/screens/data_room_lobby_screen.dart`: Lobby receptor con watermark diagonal, lista de archivos, visor inline de PDF/imagen/texto.
- `lib/screens/profile/profile_screen.dart`: navegación a `/storage-management` y muestra capacidad del Data Room.

#### Enrutamiento y seguridad
- `lib/providers/router_provider.dart`: rutas `/storage-management` y `/folder-room/:folderLinkId`; lógica de redirect preserva deep links de carpetas.
- `lib/main.dart`: deep links `folder-room/` enrutan al Lobby.
- `lib/main.dart`: `ScreenshotService.enableSecureView()` global tras inicializar.
- `lib/screens/viewer/viewer_screen.dart`: ya no deshabilita FLAG_SECURE en dispose.

#### Dependencias
- `pubspec.yaml`: agregado `purchases_flutter: ^8.0.0`.

#### Modo prueba Premium (sin RevenueCat/Google Cloud)
- `lib/providers/auth_provider.dart`: método `setPremiumSimulation(bool)` que actualiza `subscription_tier` y `max_storage_bytes` directamente en Supabase.
- `lib/features/data_room/presentation/screens/storage_management_screen.dart`: en builds debug aparece una sección "MODO PRUEBA" con botón para activar/desactivar Premium de prueba.
- Esto permite evaluar la interfaz Premium y probar subidas de >10 MB sin configurar RevenueCat ni Google Cloud.

#### Corrección post-compilación
- `lib/features/data_room/presentation/screens/data_room_lobby_screen.dart`: se eliminó `_pdfController.dispose()` porque `PdfViewerController` no expone ese método en `pdfrx ^1.3.5`.
- Se mantiene advertencia de plugins `purchases_flutter` y `share_plus` que aplican Kotlin Gradle Plugin (KGP); es un warning no bloqueante en Flutter actual.

#### Soporte de video (MP4 y formatos comunes)
- `pubspec.yaml`: agregado `video_player: ^2.9.1`.
- `lib/widgets/video_player_screen.dart`: nuevo `SecureVideoPlayerScreen` que escribe bytes descifrados a un archivo temporal privado, reproduce con el reproductor nativo y elimina el archivo temporal al cerrarse.
- `lib/screens/viewer/viewer_screen.dart`: detecta `mimeType.startsWith('video/')` y ofrece reproducir el video con `SecureVideoPlayerScreen`.
- `lib/features/data_room/presentation/screens/data_room_lobby_screen.dart`: icono de video y reproducción en el Lobby de carpetas.
- `lib/providers/file_provider.dart` y `lib/features/data_room/presentation/notifiers/folder_notifier.dart`: timeouts de Dio aumentados (`connectTimeout: 30s`, `receive/sendTimeout: 5min`) para archivos grandes.

#### Limitaciones de video
- La reproducción depende de los codecs que soporten Android/iOS nativamente. MP4/H.264 funciona en prácticamente todos los dispositivos. MKV, AVI y otros codecs específicos pueden no reproducirse.
- El video descifrado se escribe temporalmente en el directorio privado de caché de la app y se borra al cerrar el reproductor.

### Pendiente / Acción requerida del usuario
1. Ejecutar `supabase/migrations/20260725000000_premium_dataroom.sql` en Supabase SQL Editor.
2. Ejecutar `flutter pub get` y `flutter run`.
3. Para probar Premium sin RevenueCat:
   - Ir a **Perfil** → **Gestionar Bóveda Premium**.
   - En la sección "MODO PRUEBA (debug)", presionar **Activar Premium de prueba**.
   - Esto actualiza `subscription_tier = 'premium'` y `max_storage_bytes = 1 GB` en Supabase.
4. Probar:
   - Subir archivo >10 MB y ≤100 MB.
   - Ver que el slider de duración permite hasta 720 horas.
   - Crear carpeta manualmente en Supabase, asignar archivos, generar share_link.
   - Abrir `/folder-room/<share-link-id>` en segundo dispositivo.

### Limpieza posterior
- Antes de publicar en producción, eliminar o desactivar la sección "MODO PRUEBA" de `storage_management_screen.dart` (solo aparece en `kDebugMode`, así que no afecta release builds).
- Reemplazar la API key de `revenue_cat_service.dart` y configurar RevenueCat + webhook para pagos reales.

## Notas
- El entorno del agente sigue sin poder ejecutar `flutter run` ni comandos SQL contra la base remota, así que los cambios son de código/DDL y dependen de que el usuario los aplique y pruebe.
- Quedan archivos huérfanos (`lib/services/secure_credential_service.dart` y `lib/screens/biometric/biometric_lock_screen.dart`) que no se pueden eliminar desde este entorno Windows con Bash roto; no causan errores porque no se importan.
